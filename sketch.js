var weight = [30, 50, 40, 60]
var sum = weight[0] + weight[1] + weight[2] + weight[3]
var average = sum / weight.length
console.log(average)
function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(51);

}

